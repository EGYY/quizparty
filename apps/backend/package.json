{
  "name": "@quizparty/backend",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "lint": "eslint .",
    "start": "node dist/main.js",
    "start:dev": "nest start --watch",
    "prisma:generate": "set -a; if [ -f ../../.env ]; then . ../../.env; else . ../../.env.example; fi; set +a; prisma generate --schema prisma/schema.prisma",
    "prisma:migrate": "set -a; if [ -f ../../.env ]; then . ../../.env; else . ../../.env.example; fi; set +a; prisma migrate dev --schema prisma/schema.prisma --name init",
    "prisma:studio": "set -a; if [ -f ../../.env ]; then . ../../.env; else . ../../.env.example; fi; set +a; prisma studio --schema prisma/schema.prisma",
    "prisma:seed": "set -a; if [ -f ../../.env ]; then . ../../.env; else . ../../.env.example; fi; set +a; tsx prisma/seed.ts"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@nestjs/platform-socket.io": "^11.1.19",
    "@nestjs/websockets": "^11.1.19",
    "@prisma/client": "^6.0.0",
    "@quizparty/shared": "workspace:*",
    "bullmq": "^5.76.6",
    "ioredis": "^5.4.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "socket.io": "^4.8.3",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@types/node": "^22.0.0",
    "prisma": "^6.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.3"
  }
}
