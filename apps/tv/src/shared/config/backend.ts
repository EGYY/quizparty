/**
 * Backend URL configuration.
 * React Native does not read .env files at runtime — edit this file directly.
 *
 * Examples:
 *   'https://quizpartygame.ru'  — production VPS
 *   'http://79.137.192.41:3001'  — production VPS IP
 *   'http://10.0.2.2:3001'     — Android emulator → localhost:3001
 *   'http://192.168.1.5:3001'  — physical device on the same Wi-Fi
 */
export const BACKEND_ORIGIN = 'https://quizpartygame.ru';
