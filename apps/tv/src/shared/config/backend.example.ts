/**
 * Copy this file to backend.ts and set your backend URL.
 * React Native does not read .env files — configure the URL here.
 *
 * Examples:
 *   'https://quizpartygame.ru'  — production VPS
 *   'http://10.0.2.2:3001'     — Android emulator → localhost:3001
 *   'http://192.168.1.5:3001'  — physical device on the same Wi-Fi
 */
export const BACKEND_ORIGIN = 'http://10.0.2.2:3001';
